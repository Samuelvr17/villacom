[build]
builder = "nixpacks"
buildCommand = "python -m pip install -r requirements.txt"

[deploy]
startCommand = "python manage.py migrate && gunicorn store.wsgi --log-file - --bind 0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
